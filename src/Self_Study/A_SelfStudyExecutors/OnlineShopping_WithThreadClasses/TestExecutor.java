package Self_Study.A_SelfStudyExecutors.OnlineShopping_WithThreadClasses;


public class TestExecutor {

    public static void main(String[] args) {
        System.out.println("Sipariş işlemleri başlatılıyor...");
/*
        // Executor nesnesi oluşturuluyor
        OnlineShoppingExecutor executor = new OnlineShoppingExecutor();

        // Fatura oluşturma işlemi
        executor.execute(new FaturaOlusturmaThread());

        // Ödeme işlemi
        executor.execute(new OdemeIslemiThread());

        // Kargo hazırlık işlemi
        executor.execute(new KargoHazirligiThread());

        System.out.println("Sipariş işlemleri devam ediyor...");

 */
    }
}
